<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripcrew.user.model.mapper.UserMapper">

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM users WHERE email = #{email})
    </select>

    <select id="findByEmail" resultType="User">
        SELECT id, email, password, nickname, role, status, created_at, updated_at
          FROM users
         WHERE email = #{email}
    </select>

    <select id="findById" resultType="User">
        SELECT id, email, password, nickname, role, status, report_count, created_at, updated_at
          FROM users
         WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, nickname, role)
        VALUES (#{email}, #{password}, #{nickname}, #{role})
    </insert>

    <!-- 관리자용 전체 사용자 목록. password 는 노출하지 않으려 SELECT 에서 제외(null 로 둠). -->
    <select id="findAll" resultType="User">
        SELECT id, email, nickname, role, status, report_count, created_at, updated_at
          FROM users
         ORDER BY id ASC
    </select>

    <!-- 관리자 대시보드 집계: 전체/상태별 사용자 수 -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM users WHERE status = #{status}
    </select>

    <!-- 관리자용 role 변경. affected rows 0 이면 대상 사용자 없음. -->
    <update id="updateRole">
        UPDATE users
           SET role = #{role}
         WHERE id = #{id}
    </update>

    <!-- 관리자용 계정 상태(밴/해제) 변경. affected rows 0 이면 대상 사용자 없음. -->
    <update id="updateStatus">
        UPDATE users
           SET status = #{status}
         WHERE id = #{id}
    </update>

    <update id="updateNickname">
        UPDATE users
           SET nickname = #{nickname}
         WHERE id = #{id}
    </update>

    <!-- 신고 처리완료 시 누적 신고 횟수 +1 -->
    <update id="incrementReportCount">
        UPDATE users
           SET report_count = report_count + 1
         WHERE id = #{id}
    </update>

</mapper>
